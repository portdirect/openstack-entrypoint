============
Installation
============

At the command line::

    $ pip install openstack-entrypoint

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv openstack-entrypoint
    $ pip install openstack-entrypoint
